#ifndef PQ_HPP
#define PQ_HPP
#include "types.hpp"
//#include "utils.hpp"

void pq( coord       *       Fattr,
         coord       * const Y,
         matval const * const p_sp,
         matidx       *       ir,
         matidx       *       jc,
         int    const         n,
         int    const         d);

#endif
